# Лабораторная работа № 1. Реализация архитектуры NPC на основе FSM и Behavior Tree

Дисциплина «Игровой искусственный интеллект» · Максимум **20 баллов** (+5 за задание со звёздочкой)

**Студент:** ФИО, группа · **Вариант стека:** … · **Видео:** <ссылка> · **Отчёт:** `docs/report.md` → PDF

## Стек

один из вариантов:
Вариант A (рекомендуемый): Unity 2022.3 LTS + C#, NavMesh, готовые ассеты Standard Assets.
Вариант B: Godot 4 + GDScript + NavigationAgent3D.
Вариант C: Python + pygame + кастомная 2D-сцена (для тех, кто не работает с Unity/Godot).

## Что нужно сдать

- [ ] Сцена со стражником в двух режимах (FSM / BT, переключение в инспекторе)
- [ ] Диаграммы FSM и BT в отчёте
- [ ] Отчёт

Полное задание, критерии оценки и типичные ошибки — в методических указаниях (ЛР № 1).

## Варианты в этом шаблоне

- `unity/` — вариант A: скрипты FSM, BT, сенсоров и EditMode-тесты (скопировать в `UnityProject/Assets/_Project/`).
- `python/` — вариант C: та же архитектура на pygame.

Оставьте один вариант, второй удалите в первом коммите.

## Запуск (Python)

```bash
cd python && pip install -r requirements.txt   # или: make install
make check                        # линтер + тесты; все команды: make help
```

## Как сдавать

1. Работайте в этом репозитории, коммитьте по шагам (`step-1`, `step-2` …) — история коммитов учитывается.
2. Отчёт пишите в `docs/report.md` (черновик по разделам, 4–6 стр.), затем перенесите в официальный шаблон отчёта, принятый на кафедре, и экспортируйте в PDF (`docs/report.pdf`).
3. Видео — на YouTube/Диск, ссылку в README и в отчёт. Файлы видео в git не кладём.
4. Готовую работу отметьте тегом `git tag v1.0 && git push --tags` и создайте Release.


## Постановка задачи
Реализовать поведение NPC-стражника, патрулирующего набор waypoints в 3D-сцене Unity или 2D-проекте на Python/pygame. Стражник должен:
- [ ] Идти от waypoint к waypoint в режиме патруля.
- [ ] При обнаружении игрока (raycast в FOV без препятствий) переходить в режим тревоги (Alert).
- [ ] После 1 секунды в Alert при сохранении видимости — начать преследование (Chase).
- [ ] При попадании игрока в зону атаки (1.5 м) — атаковать.
- [ ] При потере цели на N секунд — переходить в Search и обходить «последнюю известную позицию».
- [ ] Если в Search не нашёл цель за 10 секунд — возвращаться к патрулю.

##  Подготовка сцены (Пример)
Создать сцену с прямоугольной комнатой 20×20 м, расставить 4 препятствия.
Добавить 4 waypoints в виде Empty/Node3D-объектов.
Добавить NPC-стражника (капсула + NavMeshAgent) и игрока (контроллер от первого/третьего лица).
Запечь NavMesh.

